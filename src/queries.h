#include <stdio.h>


/*devolve ficheiro de texto (ver guião)*/ q1(FILE);
/*devolve ficheiro de texto (ver guião)*/ q2(FILE);
/*devolve ficheiro de texto (ver guião)*/ q3(FILE);
/*devolve ficheiro de texto (ver guião)*/ q4(FILE);
/* ... */
 queries(const *char file_name);