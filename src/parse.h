#ifndef PARSE_H
#define PARSE_H

/*retorna a hashtable?*/ parse_users(const *char "users.csv");
/*retorna a hashtable?*/ parse_drivers(const *char "drivers.csv");
/*retorna a hashtable?*/ parse_rides(const *char "rides.csv");

#endif